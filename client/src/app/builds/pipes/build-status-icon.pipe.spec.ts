import { BuildStatusIconPipe } from './build-status-icon.pipe';

describe('BuildStatusIconPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildStatusIconPipe();
    expect(pipe).toBeTruthy();
  });
});
